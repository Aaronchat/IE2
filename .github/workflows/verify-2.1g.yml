name: Verify Infinite Engine 2.1G
on:
  push:
    branches: [ie-2.1g-location-reorganization]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node --test
